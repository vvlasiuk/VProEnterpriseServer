# Основні таблиці системи
version: "1.0.0"

tables:
  # Ролі користувачів
  cat_roles:
    parent: "catalog"
    description: "Roles"
    columns:
      permissions:
        type: "NTEXT"
        nullable: true
        comment: "JSON з дозволами"
