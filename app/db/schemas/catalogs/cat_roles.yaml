# Основні таблиці системи
version: "1.0.0"
description: "Core system tables"

tables:
  # Ролі користувачів
  cat_roles:
    parent: "catalog"
    columns:
      permissions:
        type: "NTEXT"
        nullable: true
        comment: "JSON з дозволами"
