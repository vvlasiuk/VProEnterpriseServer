# Основні таблиці системи
version: "1.0.0"

tables:
  # Ролі користувачів
  sys_roles:
    description: "Roles"
    parent: "system"
    columns:
      permissions:
        type: "NTEXT"
        nullable: true
        comment: "JSON з дозволами"
